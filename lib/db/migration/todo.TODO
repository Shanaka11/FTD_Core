Create methods to generate actual mehtods that are used in the script generated (Think .cre) files
Create methods to 
- check if the table exists
- create table
- alter table
- check if the column exists
- update column
- add new column

Should consider creating a build tool, for instance ftd_core --mig should create a script based on the modle file and the execute it